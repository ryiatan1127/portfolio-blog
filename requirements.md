# 个人作品集 + 博客网站 — 需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.1（评审修订） |
| 日期 | 2026-08-25 |
| 状态 | 已评审 |
| 作者 | 待填 |

> 修订记录（v1.2，可行性评审）：FR-5.2 补充导航项（归档/搜索，与设计/实现对齐）；FR-3.9 明确归档为「按年份聚合 + 标签云」；验收标准 9 注明展示字体对拉丁字符生效、中文标题回退系统字体。
> 修订记录（v1.1）：FR-3.5 提升为 P0/Must（与验收标准 3 对齐）；新增 FR-5.5 移动端导航、FR-5.6 每页唯一 h1；首期内容扩充（§5.3）；新增图片资源约定（§5.4）。

---

## 1. 项目概述

### 1.1 背景

需要一个能**上线**的个人网站，用于展示个人作品与写作、建立个人品牌。区别于普通静态简历，它要**好看、可交互、有特色**，同时成本要**最经济**（托管 $0 起步）。

### 1.2 目标

- 上线一个个人作品集/品牌站 + 博客，托管成本 $0（可选域名 ~¥70/年）。
- 具备三类动效：**滚动叙事**、**粒子/微交互**；博客同样带炫酷动效但保持阅读舒适。
- 内容（作品、文章）与代码分离，更新内容无需改代码。

### 1.3 一句话定位

> 一个「有生命力」的个人作品集 + 博客，让访客在滚动与交互中记住你。

---

## 2. 目标用户与使用场景

### 2.1 目标用户

| 用户 | 诉求 |
|---|---|
| 潜在雇主 / 合作方 / 客户 | 快速了解「我是谁、做过什么、能力如何」 |
| 技术/兴趣读者 | 阅读博客文章，获得内容价值 |
| 网站所有者（用户本人） | 方便地写文章、更新作品，低成本维护 |

### 2.2 核心使用场景

1. 访客首次打开首页，3 秒内了解「我是谁、做什么」。
2. 访客滚动浏览，看到经历时间线与精选作品。
3. 读者点击进入博客，舒适地阅读一篇文章。
4. 所有者新增一篇 `.mdx` 文章，push 后自动上线。
5. 所有者在移动端打开，页面正常浏览（响应式）。

---

## 3. 功能需求（FR）

> 优先级标注：**P0**（必须有）/ **P1**（应该有）/ **P2**（可以有）

### FR-1 首页

| ID | 需求 | 优先级 |
|---|---|---|
| FR-1.1 | 首屏 Hero：头像/emoji + 姓名 + 一句定位 slogan + 粒子背景动效 | P0 |
| FR-1.2 | 「关于我」经历时间线区块，滚动叙事动效逐条显现 | P0 |
| FR-1.3 | 精选作品区：展示 featured 项目（最多 3 个）+「查看全部」入口 | P0 |
| FR-1.4 | 精选文章区：展示最新文章（最多 3 篇）+「查看全部」入口 | P0 |
| FR-1.5 | 页脚：联系/社交链接（GitHub、Email 等） | P0 |

### FR-2 作品集

| ID | 需求 | 优先级 |
|---|---|---|
| FR-2.1 | 作品列表页 `/projects`：卡片展示标题、描述、技术栈标签、演示/源码链接 | P0 |
| FR-2.2 | 项目卡片 hover 动效（抬升/发光） | P1 |

### FR-3 博客

| ID | 需求 | 优先级 |
|---|---|---|
| FR-3.1 | 文章列表页 `/blog`：按日期倒序，卡片展示标题、日期、摘要、标签 | P0 |
| FR-3.2 | 文章详情页 `/blog/[slug]`：渲染 MDX 正文（标题/段落/代码高亮/链接/图片） | P0 |
| FR-3.3 | 文章通过本地 `.mdx` 文件管理，frontmatter 含 `title/date/description/tags/cover/draft/series` | P0 |
| FR-3.4 | `draft: true` 的文章构建时不发布 | P0 |
| FR-3.5 | 阅读进度条（顶部随滚动增长；与验收标准 3 对齐） | P0 |
| FR-3.6 | 博客区炫酷动效（列表卡片动效 + 文章页头部 + 段落滚动淡入） | P1 |
| FR-3.7 | 标签分类 + 筛选：文章带标签，标签页/筛选查看该标签下所有文章 | P0 |
| FR-3.8 | 文章目录 TOC：长文自动生成目录，点击跳转到小节 | P0 |
| FR-3.9 | 归档页：按年份聚合 + 标签云，纵览全部文章（标签筛选见标签页） | P1 |
| FR-3.10 | 文章系列：把相关文章串成一个系列（如「从零学 XX」） | P2 |
| FR-3.11 | 上一篇/下一篇：文末导航到相邻文章 | P0 |
| FR-3.12 | 相关文章推荐：按共享标签推荐相似文章 | P1 |
| FR-3.13 | 阅读时长 + 字数：文首显示「约 X 分钟 · N 字」 | P0 |
| FR-3.14 | 代码高亮增强：复制按钮、行号、语言标注 | P0 |
| FR-3.15 | 分享按钮：复制链接 / 社交平台分享 | P1 |
| FR-3.16 | 评论：基于 Giscus（GitHub Discussions，免费） | P1 |
| FR-3.17 | 站内搜索：客户端搜索标题/摘要/正文 | P1 |

### FR-4 动效（跨页）

| ID | 需求 | 优先级 |
|---|---|---|
| FR-4.1 | 滚动叙事：区块滚入视口时淡入 + 位移，带延迟错落 | P0 |
| FR-4.2 | 粒子背景：漂浮粒子 + 鼠标悬停连线 + 点击爆粒子 | P0 |
| FR-4.3 | 微交互：导航滚动变色、卡片 hover、页面切换淡入 | P1 |

### FR-5 通用

| ID | 需求 | 优先级 |
|---|---|---|
| FR-5.1 | 响应式：桌面与移动端均可正常浏览，导航不溢出 | P0 |
| FR-5.2 | 站内导航：首页 / 作品 / 博客 / 归档 / 搜索 | P0 |
| FR-5.3 | SEO：每页 title/description、openGraph、sitemap | P1 |
| FR-5.4 | 静态部署：`output: 'export'`，可部署 Vercel / Cloudflare Pages，$0 | P0 |
| FR-5.5 | 移动端导航：`<768px` 折叠为汉堡菜单，可展开全部导航项 | P0 |
| FR-5.6 | 每页唯一 `h1`（页面级标题用 h1，区块标题用 h2） | P1 |

---

## 4. 非功能需求（NFR）

| ID | 类别 | 需求 |
|---|---|---|
| NFR-1 | 性能 | 首页 LCP 目标 < 2.5s；粒子背景不阻塞滚动；静态站点无服务端延迟 |
| NFR-2 | 成本 | 托管 $0；唯一可选支出为域名 ~¥70/年 |
| NFR-3 | 可访问性 | 语义化标签、图片有 `alt`、正文/次要文字颜色对比达标、链接可读 |
| NFR-4 | 可维护性 | 内容与代码分离：文章用 `.mdx`、项目用数据文件，更新无需改组件代码 |
| NFR-5 | 可读性 | 博客正文排版舒适（行高 1.6、正文宽度受限、代码高亮），动效不干扰阅读 |
| NFR-6 | 兼容性 | 支持近两年主流现代浏览器（Chrome/Edge/Safari/Firefox） |
| NFR-7 | 安全性 | 纯静态、无后端、无数据库，无注入面 |
| NFR-8 | 可扩展性 | 动效、配色、字体通过 token/组件抽象，后续可加 3D、暗色主题等 |
| NFR-9 | 字体风格 | 展示性文字（Hero/标题）用丰富、花哨的字体；正文保持可读 |
| NFR-10 | 自定义字体 | 预留字体文件目录，`@font-face` 同时声明 `.woff2/.ttf/.otf` 三种格式，任一放入 `public/fonts/` 即用，无需改代码 |

### 4.1 视觉风格方向（Visual Direction）

- **配色**：现代化；**多巴胺色系但低饱和**——色彩丰富、活泼、治愈，但整体柔和、不刺眼（偏奶油/莫兰迪感的低饱和彩度）。具体色值在 mockup 阶段定稿，由 CSS token（`--accent` 等）承载。
- **风格**：**轻亚风格**——柔和、梦幻、略带 Y2K 复古感，不厚重、不暗黑。
- **装饰元素**：页面点缀可爱小元素（**小翅膀、音符、星星、爱心、泡泡**等），作为氛围点缀、不喧宾夺主。
- **卡片**：圆角偏小；**玻璃拟态（毛玻璃）**——半透明 + 背景模糊（backdrop-blur）+ 细边框 + 柔和阴影。

> 配色、字体、装饰图标的视觉定稿，在实现前的 mockup 阶段与用户逐版确认。

---

## 5. 内容需求

### 5.1 博客文章（`.mdx` frontmatter）

```yaml
title: string        # 标题
date: string         # YYYY-MM-DD
description: string  # 摘要（列表页 + SEO）
tags: string[]       # 标签
series?: string      # 可选，文章系列名（同名的串成一个系列）
cover?: string       # 可选封面图
draft?: boolean      # true 时不发布
```

### 5.2 项目数据

```ts
type Project = {
  slug: string; title: string; description: string; tags: string[];
  image?: string; links: { demo?: string; repo?: string }; featured: boolean;
}
```

> **项目数量与截图**：项目数量未定——数据驱动，可随时增删，无需改组件代码。截图仅部分项目有：无截图的项目，卡片用「渐变色块 + 首字母/emoji」占位，后续补图即可。

### 5.3 首期内容（占位）

- 至少 **3 篇**示例文章：其中至少 2 篇共享标签、至少 2 篇属于同一系列、至少 1 篇 `draft: true`——保证标签筛选、相关文章、上一篇/下一篇、系列页、归档页均有可见效果，且能验证草稿不发布。
- 至少 **2 个**示例项目：1 个 `featured: true`（验证首页精选）、1 个 `featured: false`（验证列表页与首页「查看全部」）。

### 5.4 图片资源约定

- 图片统一放在 `public/images/`，文章 `cover`、正文与项目数据均用 `/images/...` 根路径引用（如 `/images/hello-cover.png`）。
- 不引用站外图片 URL；图片尽量压缩（静态站点直接对外提供原图）。

---

## 6. 约束与假设

- **技术栈限定**：React / Next.js（用户偏好）。
- **内容作者会写代码**：会用 git + Markdown，能接受「写 `.mdx` → push」的发布流程。
- **主要省钱**：优先 $0 托管，愿为此接受静态站点的限制（无后端）。
- **语言**：站内文案简体中文为主。
- **字体授权**：自托管字体须有允许网页嵌入（`@font-face`）的授权；花哨展示字体主要用于标题/英文，正文中文用系统字体以保证可读性与加载速度。

---

## 7. 验收标准（Definition of Done）

1. 访问首页，能看到 Hero → 关于我 → 精选作品 → 精选文章 → 页脚，顺序正确。
2. 作品集页展示全部项目，卡片 hover 有抬升/发光动效。
3. 博客列表按日期倒序；点进文章正文渲染正确（标题/段落/代码块）；阅读进度条随滚动增长。
4. 新增一篇 `.mdx` 文章，`npm run build` 后静态产物中包含该文章页面。
5. 全站响应式，移动端导航不溢出、可通过汉堡菜单访问全部导航项，内容可正常浏览。
6. 粒子、滚动叙事、微交互动效流畅，无明显卡顿。
7. 构建成功产出静态文件（`out/`），可部署到 Vercel / Cloudflare Pages。
8. 每页有唯一 `h1`、`title`/`description`、`sitemap.xml` 生成。
9. 将字体文件放入指定目录后，无需改代码即可在标题/展示文字使用该字体（展示字体主要作用于拉丁字符，中文标题回退系统字体以保证可读性）。

---

## 8. 优先级划分（MoSCoW）

- **Must（必须）**：首页（FR-1）、作品集（FR-2）、博客基础（FR-3.1~3.4）、阅读进度条（FR-3.5，验收标准 3 要求）、标签分类（FR-3.7）、目录 TOC（FR-3.8）、上一篇/下一篇（FR-3.11）、阅读时长+字数（FR-3.13）、代码高亮增强（FR-3.14）、核心动效（FR-4.1/4.2）、响应式（FR-5.1/5.2）、移动端导航（FR-5.5）、静态部署（FR-5.4）。
- **Should（应该）**：博客炫酷动效（FR-3.6）、归档页（FR-3.9）、相关文章推荐（FR-3.12）、分享按钮（FR-3.15）、评论 Giscus（FR-3.16）、站内搜索（FR-3.17）、微交互（FR-4.3）、卡片 hover（FR-2.2）、SEO 与每页唯一 h1（FR-5.3/5.6）。
- **Could（可以）**：文章系列（FR-3.10）、暗/亮主题切换、更多微交互细节。
- **Won't（本期不做）**：游戏化彩蛋、3D/WebGL、多语言、PWA、RSS。

---

## 9. 范围外（Out of Scope）

- ❌ 后端 / 数据库 / 用户系统 / CMS。
- ❌ 3D / WebGL 场景（作为后续升级方向）。
- ❌ 游戏化彩蛋（用户明确暂不做）。
- ❌ 多语言、PWA、RSS（按需后续加）。
